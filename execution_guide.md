# 两篇论文实验与写作执行手册

这份手册用于把当前研究计划推进成两篇可以写作和投稿/毕业使用的论文。

## 0. 总体策略

建议采用主次分明的推进方式：

- 论文一作为主线：**基于不确定性几何先验的大场景 Gaussian 表面重建**。
- 论文二作为前沿扩展：**面向生成视频的几何一致性约束三维重建**。

不要两篇同时从零硬推。推荐先把论文一做出完整闭环，再把论文一中的几何先验、置信度和一致性思想迁移到论文二。

## 1. 推荐时间线

### 第 1 阶段：复现与环境闭环

时间：第 1-2 周

目标：

- 跑通 3DGS 或 2DGS 的训练、渲染、评测流程。
- 至少在一个小数据集上得到可视化结果和指标。
- 建立统一实验记录表。

验收标准：

- 能从输入图像训练出 Gaussian 场景。
- 能导出渲染图、深度图或 mesh/point cloud。
- 能记录 PSNR、SSIM、LPIPS 至少一种渲染指标。
- 能固定随机种子和保存配置文件。

### 第 2 阶段：论文一核心模块

时间：第 3-8 周

目标：

- 接入深度/法线/VGGT-Ω 或 DUSt3R 几何先验。
- 实现置信度加权几何损失。
- 实现几何误差驱动的 densification/pruning。
- 完成 DTU 或 Tanks and Temples 上的主实验和消融。

验收标准：

- 有完整模型结果。
- 有至少 4 个 baseline。
- 有至少 5 组消融实验。
- 有可视化对比图：漂浮点、厚壳、边界、远距离结构。

### 第 3 阶段：论文一扩展实验与写作

时间：第 9-12 周

目标：

- 扩展到 MatrixCity、KITTI-360 或 ScanNet/ScanNet++。
- 完成论文一初稿。
- 补齐实验表格、消融、失败案例和方法图。

验收标准：

- 论文一有完整结构：摘要、引言、相关工作、方法、实验、结论。
- 至少完成 2 个数据集实验。
- 至少完成 1 张方法框架图、1 张定性对比图、2 张定量表格。

### 第 4 阶段：论文二最小可行版本

时间：第 13-18 周

目标：

- 选择生成视频或 WorldScore 样例作为输入。
- 使用 VGGT-Ω/DUSt3R/Depth Anything 提取几何。
- 实现不一致帧检测和几何一致性约束。
- 对比 COLMAP + 3DGS、VGGT-Ω + 3DGS、World Reconstruction From Inconsistent Views。

验收标准：

- 能展示生成视频直接重建失败案例。
- 能展示加入一致性约束后的改善。
- 有 cycle error、reprojection error 或 point alignment error。
- 有论文二初稿大纲和核心图表。

## 2. 论文一执行路线

论文一目标是稳：证明方法能提高真实三维几何精度。

### 2.1 第一版实验只做最小闭环

先不要一开始就做大场景。建议顺序：

1. 选择 DTU 或 Tanks and Temples 的 1-2 个场景。
2. 跑通 3DGS baseline。
3. 跑通 2DGS 或 CityGaussianV2 baseline。
4. 接入一种几何先验，优先深度或法线。
5. 实现置信度加权损失。
6. 做完整模型和消融。

### 2.2 方法模块优先级

必须完成：

- 几何先验初始化。
- 置信度加权深度损失。
- 置信度加权法线损失。
- 几何误差驱动 pruning。
- 消融实验。

有时间再做：

- 几何误差驱动 densification。
- 动态区域 mask。
- 大场景分块训练。
- VGGT-Ω/DUSt3R 多模型融合。

### 2.3 关键实验问题

实验要回答以下问题：

- 加几何先验是否真的提升 Chamfer/F1，而不是只提升 PSNR？
- 置信度加权是否比直接用深度/法线监督更稳？
- 几何 pruning 是否能减少漂浮点和厚壳？
- 在稀疏视角和远距离区域，方法是否比 3DGS/2DGS 更稳？

## 3. 论文二执行路线

论文二目标是新：解决生成视频三维不一致导致无法稳定重建的问题。

### 3.1 第一版实验只证明现象

不要一开始追求完整系统。先做三件事：

1. 找 3-5 个生成视频样例。
2. 用 COLMAP + 3DGS 或 VGGT-Ω + 3DGS 重建。
3. 展示失败：多层墙面、物体漂移、闭环错位、点云无法对齐。

这一步很重要，因为论文二的贡献首先来自清楚定义问题。

### 3.2 方法模块优先级

必须完成：

- 生成视频几何提升。
- 不一致帧/区域检测。
- 深度一致性约束。
- 法线一致性约束。
- 重投影误差或点云对齐误差指标。

有时间再做：

- 语义一致性约束。
- 循环一致性约束。
- 局部重生成或帧筛选。
- WorldScore 完整评测。

### 3.3 关键实验问题

实验要回答以下问题：

- 生成视频为什么不能直接用于 3DGS 重建？
- 哪些帧或区域导致几何崩坏？
- 一致性约束是否减少跨帧漂移和多层表面？
- 方法是否让新视角渲染更可探索，而不只是更好看？

## 4. 写作顺序

不要等实验全部做完再写。推荐边做边写：

1. 先写引言和相关工作。
2. 实验跑通后写方法。
3. 每完成一组实验就填表格和分析。
4. 最后补摘要、结论和局限性。

论文一写作主线：

- 现有 Gaussian 方法渲染强，但几何不够准。
- 单目几何先验有用，但直接使用会引入噪声。
- 本文用不确定性机制可靠地使用几何先验。
- 实验证明几何指标和可视化都更好。

论文二写作主线：

- 视频生成模型视觉质量高，但三维一致性不足。
- 直接重建会失败，因此需要先诊断和修复几何不一致。
- 本文提出生成视频到三维世界的几何一致性重建框架。
- 实验证明方法提升可探索性和跨视角一致性。

## 5. 每周固定交付物

每周至少保存：

- 一份实验配置。
- 一张定性结果图。
- 一行实验表格。
- 一段实验观察文字。
- 一个失败案例或风险记录。

建议文件命名：

- `experiments/week01_baseline.md`
- `experiments/week02_2dgs.md`
- `experiments/week03_depth_prior.md`
- `experiments/week04_confidence_loss.md`

## 6. 刹车点

如果论文一在第 6 周仍无法证明几何指标提升：

- 降低目标，不做大场景。
- 聚焦 DTU/Tanks and Temples。
- 把贡献改成“置信度先验提升几何稳定性”。

如果论文二在第 16 周仍无法形成稳定系统：

- 不强行写完整论文。
- 改成论文一的扩展章节或未来工作。
- 保留“生成视频重建失败诊断”和“初步一致性修复”作为探索性实验。

## 7. 当前建议

现在最应该做的第一件事：

**先跑通论文一的最小闭环：3DGS/2DGS baseline + 一个几何先验 + 一个小数据集。**

这一步完成后，后面所有写作和创新都会更踏实。
